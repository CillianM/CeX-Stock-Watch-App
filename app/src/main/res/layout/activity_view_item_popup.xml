<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:background="@color/white">

    <EditText
        android:id="@+id/potentialItem"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Enter a new item name..."
        android:gravity="center_horizontal"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/submit" />

    <Button
        android:id="@+id/submit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Rename"
        android:layout_centerVertical="true"
        android:onClick="updateName"
        android:layout_alignRight="@+id/potentialItem"
        android:layout_alignEnd="@+id/potentialItem"
        android:layout_alignLeft="@+id/potentialItem"
        android:layout_alignStart="@+id/potentialItem" />

    <Button
        android:id="@+id/delete"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Delete"
        android:onClick="delete"
        android:layout_below="@+id/submit"
        android:layout_alignLeft="@+id/submit"
        android:layout_alignStart="@+id/submit"
        android:layout_alignRight="@+id/submit"
        android:layout_alignEnd="@+id/submit"/>

    <Button
        android:id="@+id/dismiss"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Dismiss"
        android:onClick="done"
        android:layout_below="@+id/delete"
        android:layout_alignLeft="@+id/delete"
        android:layout_alignStart="@+id/delete"
        android:layout_alignRight="@+id/delete"
        android:layout_alignEnd="@+id/delete"/>
</RelativeLayout>
